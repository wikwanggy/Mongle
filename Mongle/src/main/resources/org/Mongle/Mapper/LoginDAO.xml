<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="org.Mongle.Mapper.LoginDAO">
  	<select id="login" resultType="org.Mongle.model.LoginDTO">
  		select * from member
  		where id=#{id} 
		and password=#{password}
  	</select>
  	<select id="logincheck" resultType="int">
  		select count(*) from member where id=#{id} and password=#{password}
  	</select>
  	<insert id="signup">
  		insert into member(id, password, name, bth, email,gender) 
	values(#{id},#{password},#{name},#{bth},#{email},#{gender});
  	</insert>
  </mapper><!--  -->